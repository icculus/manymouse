
- Hide absolute input (always convert to relative internally?)
- return -1 on init failure, instead of 0, which could mean successful init,
  but no devices available?
- Look for FIXMEs.

/* end of TODO ... */

